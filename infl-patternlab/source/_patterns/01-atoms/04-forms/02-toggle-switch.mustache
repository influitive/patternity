<section class="pattern-section">
  <header>
    <h3>toggle switch</h3>
  </header>

  <p>The toggle switch form element can be used in place of a checkbox to better visually indicate that a feature is turned on or off.  It also still acts as a form element checkbox and can be used with a form tag and form submit.</p>

  <div id="toggle-switch-pattern" style="margin-top: 10px;"></div>
  <script type="text/javascript">
    (function() {
      var props = {
        enabled: false,
        onChange : function(newValue) {
          console.log(newValue);
        },
        inputName: "test-name"
      };

      inflComp.React.render(
        inflComp.React.createElement(inflComp.ToggleSwitch, props),
        document.getElementById("toggle-switch-pattern")
      );
    })();
  </script>

  <h4 class="code-title">Code</h4>

  <h5 class="code-title">JSX</h5>
  <pre class="code">
    <code>
      &lt;ToggleSwitch enabled={false} onChange={callback_function} inputName={form_element_name} /&gt;
    </code>
  </pre>

  <h5 class="code-title">Without JSX</h5>
  <pre class="code">
    <code>
      React.render(
        React.createElement(ToggleSwitch, props),
        {DOM element to append toggle switch to}
      );
    </code>
  </pre>

  <h4 class="code-title">Props</h4>

  <pre class="code">
    <code>
      {
        enabled : boolean,    // defaults to false
        onChange : function,  // the onChange will be called with the current state of enabled as a boolean
        inputName : string    // name of the checkbox input to be used in a form
      }
    </code>
  </pre>
</section>
