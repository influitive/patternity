<section class="pattern-section">
  <header>
    <h3>action alert</h3>
  </header>

  <p>An ActionAlert has the same functionaliy as Alert except it is not closeable.</p>

  <h4>Action Alert</h4>

  <p>The alert action allows you to add a new action to an alert.  It also comes with special styling.</p>

  <div id="alert-action-pattern"></div>
  <script type="text/javascript">
    (function() {
      var props = {
        key: "alert-" + Math.random(),
        title : "Default Alert",
        children : ["Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat."],
        showIcon: true,
        // closeable: true,
        type: "success",
        action : {
          onClick : function(){},
          title: "Do Something"
        }
      };

      inflComp.React.render(
        inflComp.React.createElement(inflComp.ActionAlert, props),
        document.getElementById("alert-action-pattern")
      );
    })();
  </script>

  <h4 class="code-title">Code</h4>

  <h5 class="code-title">JSX</h5>
  <pre class="code">
    <code>
      &lt;ActionAlert {...this.props}&gt;
        &lt;p&gt;Your Body&lt;/p&gt;
        &lt;p&gt;Your Body&lt;/p&gt;
      &lt;/ActionAlert&gt;
    </code>
  </pre>

  <h5 class="code-title">Without JSX</h5>
  <pre class="code">
    <code>
      React.render(
        React.createElement(ActionAlert, props, children),
        {DOM element to append action alert to}
      );
    </code>
  </pre>

  <h4 class="code-title">Props</h4>

  <pre class="code">
    <code>
      {
        title : "Your Title",
        showIcon : boolean,           // optional defaults to false
        children: [...],              // array of text, html, react elements
        type : String                 // can be success, error, warning or ''
        showAlert : boolean           // determines if the alert is visible or not
        onClose : function,           // optional defaults to empty function called when alert is closed
        hideIn : integer,             // determines the interval until the input is hidden.  optional and defaults to don't hide
        action : {
          title : string,         // action button title
          onClock : function,     // callback function for when the action is clicked
        }
      }
    </code>
  </pre>
</section>
